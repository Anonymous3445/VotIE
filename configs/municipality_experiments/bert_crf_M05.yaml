# bert_crf — Leave-One-Municipality-Out: M05 as test set
# Generated by scripts/make_lomo_configs.py — do not edit by hand.

model:
  name: bert_crf
  model_name: neuralmind/bert-large-portuguese-cased
  architecture: crf
  dropout: 0.1
  num_labels: null
data:
  data_dir: data/citilink-votie-lomo-splits/M05
  train_file: train.jsonl
  dev_file: dev.jsonl
  test_file: test.jsonl
training:
  batch_size: 8
  epochs: 10
  learning_rate: 5e-5
  warmup_proportion: 0.1
  weight_decay: 0.01
  patience: 3
  device: 0
  apply_bio_validation: true
  seed: 42
optimization:
  optimizer: adamw
  scheduler: linear_with_warmup
  grad_clip: 1.0
augmentation:
  max_length: 512
  padding: max_length
  truncation: true
windowing:
  enable_windowing: true
  overlap_tokens: 50
