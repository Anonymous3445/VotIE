# Core ML & Transformer stack
torch>=2.0.0
transformers>=4.40.0
pytorch-crf>=0.7.2
accelerate>=0.27.0

# Data processing
numpy>=1.21.0
pandas>=1.3.0
scikit-learn>=1.0.0

# NER evaluation
seqeval>=1.2.2
sklearn-crfsuite>=0.3.6

# Utilities
pyyaml>=6.0
tqdm>=4.62.0
python-dotenv>=1.0.0
sentencepiece>=0.1.96
pydantic>=2.0.0

# Portuguese NLP
spacy>=3.4.0       # required for dataset generation scripts
fasttext           # optional: required by bilstm_fasttext baseline only

# LLM extraction (optional: required only for Gemini experiments)
google-generativeai
google-genai>=0.1.0
outlines>=0.0.40   # optional: structured output for LLM baselines
langextract[openai]>=1.1.0  # AMALIA extraction via langextract

# Optional: GPU support
# Install separately with: pip install torch --index-url https://download.pytorch.org/whl/cu118
